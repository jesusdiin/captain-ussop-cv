export async function exaggerateDescription(
  description: string
): Promise<string> {
  // Stub: returns hardcoded exaggerated text for now
  return `Lideré una iniciativa de transformación logística cross-funcional, orquestando personalmente la reubicación estratégica de activos físicos de misión crítica entre zonas operativas, resultando en una mejora del 200% en la utilización de recursos espaciales. Descripción original: "${description}"`;
}
